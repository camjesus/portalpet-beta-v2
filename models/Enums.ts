export enum Age {
    YEAR = "YEAR",
    MONTH = "MONTH"
}

export enum Sex {
    MALE = "MALE",
    FEMALE = "FEMALE"
}

export enum Action {
    WANTED = "WANTED",
    FOUND = "FOUND",
    ADOPTION = "ADOPTION"
}

export enum Type {
    DOG = "DOG",
    CAT = "CAT"
}

export enum Size {
    BIG = "BIG",
    MEDIUM = "MEDIUM",
    SMALL = "SMALL"
}